import { defineChain } from "thirdweb";

/**
 * All chains should be exported from this file
 */


/**
 * Define any custom chain using `defineChain`
 */
export const vitruveo = defineChain(1490); // don't actually use this
